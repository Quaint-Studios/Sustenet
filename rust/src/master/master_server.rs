pub struct MasterServer;

// TODO Implement

impl MasterServer {
    pub fn new() -> MasterServer {
        MasterServer
    }

    pub fn start(&self) {
        println!("Master server started");
    }
}
